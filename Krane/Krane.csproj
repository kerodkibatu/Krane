<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
	<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Nullable>enable</Nullable>
    <Authors>KerVerse</Authors>
    <PackageId>Krane</PackageId>
    <Product>Krane</Product>
    <AssemblyVersion></AssemblyVersion>
    <AssemblyName>Krane</AssemblyName>
    <Title>Krane</Title>
    <PackageIcon></PackageIcon>
    <RepositoryUrl>https://github.com/Ker-Verse/Krane</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>game-framework;SFML.NET;game-engine;media-library;games;game-dev;opengl;vulkan;gui;</PackageTags>
    <Copyright>Copyright© KerVerse 2022 Licensed under GNU General Public License v3.0</Copyright>
    <Version>0.3.1-alpha</Version>
    <Description>Krane is a graphics framework for C# built on top of SFML.NET. It allows for fast and reliable graphics rendering on Windows. The core functionality is based on a .NET port of SFML (Simple Fast Multimedia Library). Krane can create a window with custom functions to control FPS, Set Title and many more upcoming  features. Krane can even be used to make games . This project has been tested with .NET6 and is compatible with .NET Standard  2.0 ,You can find the repository on https://www.github.com/Ker-Verse/Krane . You can find a README for the project in the GitHub repo.</Description>
    <PackageReleaseNotes>What's New?
- Added new events to the game class

Bug Fixes and Improvements 
None

Coming Soon
- Panel widget
</PackageReleaseNotes>
    <PackageOutputPath>..\nuget\</PackageOutputPath>
    <PackageLicenseFile>LICENSE.txt</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>True</PackageRequireLicenseAcceptance>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\LICENSE.txt">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SFML.Net" Version="2.5.0" />
  </ItemGroup>

</Project>
